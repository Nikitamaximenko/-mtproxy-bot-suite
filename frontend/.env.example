# Backend API URL. In production (Railway/Vercel) set to your backend, e.g.:
# BACKEND_URL=https://mtproxy-bot-suite-production.up.railway.app
BACKEND_URL=http://localhost:8000

# Второй поток оплаты (Prodamus / СБП в мини-аппе). Пока не готово — не задавайте или false.
# Для включения: true (и на бэкенде ENABLE_PRODAMUS_CHECKOUT=true).
# NEXT_PUBLIC_ENABLE_PRODAMUS_CHECKOUT=true

